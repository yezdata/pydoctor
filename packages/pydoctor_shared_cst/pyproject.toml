[project]
name = "pydoctor-shared-cst"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "libcst>=1.8.6",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
  
[tool.hatch.build.targets.wheel]
packages = ["src/pydoctor_shared_cst"]